//----------------------------SOME------------------------------------


/*
    //return true if one of the condition of return statement is true else return false


    //check if all numbers is evenNumbers or not
    const evenNumbers=[2,4,6,8,7]
    const someValue=evenNumbers.some((num)=>{
    return num%2===1
    })

    console.log(someValue)

*/

//----------------------------EVERY------------------------------------


 
    //return true if all of the condition of return statement is true else return false

    /*
        const evenNumbers=[2,4,6,8,1]
        //check all numbers are even
        const someValue=evenNumbers.every((num)=>{
        return num%2==0
        })

        console.log(someValue)   //return  false because one number is odd

    */

    /*

        const evenNumbers=[2,4,6,8,10]
        //check all numbers are even
        const someValue=evenNumbers.every((num)=>{
        return num%2==0
        })

        console.log(someValue)   //return true because every number is even

    */
